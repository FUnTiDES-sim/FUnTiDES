add_subdirectory(api)
add_subdirectory(impl)
add_subdirectory(pywrap)
if(COMPILE_DG)
  add_subdirectory(DG)
endif()
if(COMPILE_DG_SEM)
  add_subdirectory(DG-SEm)
if(COMPILE_DG_PADAPTIVE)
  add_subdirectory(DG_padaptive)
endif()
