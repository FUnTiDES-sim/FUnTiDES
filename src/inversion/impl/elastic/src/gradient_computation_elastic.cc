#include "gradient_computation_elastic.h"

#include <stdexcept>
#include <vector>

namespace inversion
{

// Explicit template instantiations will be provided in a separate file
// or linked from the inversion library

}  // namespace inversion
