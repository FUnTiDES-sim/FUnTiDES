Delegate the directory $ARGUMENTS to the comment-writer subagent. When it returns, run
`scripts/check_comments_only.sh`, then show me its summary and `git diff --stat`.
Do not build the project. Do not commit.
